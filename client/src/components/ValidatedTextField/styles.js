import { amber600 } from 'material-ui/styles/colors';

const styles = {
  fieldStyle: {
    width: '100%',
  },
  errorStyle: {
    color: amber600,
    position: 'absolute',
    bottom: '-0.42rem',
  },
  underlineStyle: {
    borderColor: amber600,
  },
};

export default styles;
